# 배포 및 운영 안내

한 번 배포한 뒤에는 손댈 일이 거의 없도록 만든 구조와, 그 이유를 적어둔다.

## 어디에 배포하나 — Railway

**Vercel에는 배포할 수 없다.** 취향 문제가 아니라 구조가 맞지 않는다.

- 네이버·티스토리는 공개 API가 없어서 실제 브라우저(Chromium)를 띄워 로그인하고
  글을 쓴다. 한 건에 30~60초가 걸린다. Vercel 함수는 최대 실행시간이 훨씬 짧고
  브라우저를 띄울 수 없다.
- 예약 발행·매일 자동 리포트는 서버가 계속 떠 있어야 동작한다.
  Vercel은 요청이 올 때만 잠깐 실행되는 구조라 상주 스케줄러를 둘 수 없다.

Railway는 컨테이너가 계속 떠 있고 Dockerfile을 그대로 쓸 수 있어서 둘 다 된다.

## 처음 한 번만 하는 일

1. **Railway에 배포** — 이 저장소를 연결하면 `Dockerfile`로 알아서 빌드된다.
   (`railway.toml`에 시작 명령·헬스체크·재시작 정책이 들어 있다)

2. **필수 키 입력** — 대시보드 `설정` 탭 또는 `/setup` 에서 넣는다.
   Railway 환경변수를 직접 건드릴 필요는 없다.

   | 키 | 용도 | 없으면 |
   |---|---|---|
   | `ANTHROPIC_API_KEY` | 글 생성 | 글이 만들어지지 않는다 (가장 중요) |
   | `UNSPLASH_ACCESS_KEY` | 본문 사진 | 주제와 무관한 사진이 들어간다 |
   | `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET` | 키워드 수집·상품 추천 | 제휴 추천이 비어 있다 |

3. **구글 블로그 연결** — `/setup` → `구글 계정 연결 시작`.
   구글 클라우드 콘솔에서 두 가지를 먼저 맞춰야 한다.
   - 승인된 리디렉션 URI 에 `https://<주소>/oauth/blogger/callback` 추가
   - OAuth 앱을 **프로덕션**으로 전환 (테스트 상태면 구글이 7일마다 토큰을 만료시킨다)

4. **연결이 안 끊기게 고정** — `설정` 탭 → `🔒 다시는 안 끊기게` → `복구값 만들기` →
   복사해서 Railway → Variables 에 **`CREDENTIALS_BLOB`** 이름으로 붙여넣기.

   Railway는 재배포할 때마다 컨테이너를 새로 만들기 때문에 서버에 저장된 값이 사라진다.
   이게 "어제는 됐는데 오늘은 연결이 끊겼다"의 주된 원인이었다.
   환경변수는 재배포해도 지워지지 않으므로, 한 번 붙여넣으면 그 뒤로는 유지된다.
   (볼륨을 붙여도 된다: 서비스 우클릭 → Add Volume → Mount path `/data`)

여기까지 하면 끝이다. 이후 코드를 고쳐 배포해도 다시 연결할 일이 없다.

## 아이폰에서 쓰기

사파리로 대시보드를 열고 → 공유 버튼 → **홈 화면에 추가**.

주소창 없이 앱처럼 열리고, 아이콘도 붙는다. 신호가 약한 곳에서도 화면은 뜨지만,
발행 상태 같은 데이터는 항상 서버에서 새로 받아온다(오래된 상태를 보여주지 않는다).

## 뭔가 안 될 때 보는 곳

순서대로 보면 대부분 여기서 끝난다.

1. `설정` → **🧾 서버가 겪은 오류** → `불러오기`
   맨 위 "서버 가동 N분"이 5분 미만이면 **방금 재시작된 것**이다.
   재배포했거나 서버가 죽었다 살아난 경우이고, 연결이 끊겼다면 원인은 대개 이것이다.
2. `설정` → **🚀 진짜 발행 테스트**
   짧은 글을 실제로 올려본다. AI 생성을 건너뛰므로,
   문제가 *글 생성*인지 *발행*인지 바로 갈린다.
3. `/setup` → **단계별 진단**
   구글 연결이 1~4단계 중 어디서 막혔는지, 값이 어디서 왔는지(환경변수인지
   화면에서 저장한 것인지)까지 알려준다.

## 알아서 돌아가는 것들 (손댈 필요 없음)

### 자가 점검

매시간 스스로 점검한다. 고칠 수 있는 건 조용히 고치고,
**사람이 손대야 하는 것만** 알린다. 같은 문제로는 하루 한 번까지만 알린다
— 알림이 잦으면 결국 안 보게 되기 때문이다.

밤 9시에 하루 요약이 한 통 온다. 문제가 없으면
"오늘 N건 발행 완료 / 신경 쓰실 것 없습니다"로 끝난다.

알림을 받으려면 `설정` 또는 `/setup` 에서 텔레그램 봇 토큰과 챗 ID를 넣으면 된다
(@BotFather 에서 봇 생성, @userinfobot 에서 내 ID 확인). 안 넣어도 동작은 하지만,
그러면 대시보드를 직접 열어봐야 한다.

### 대시보드 첫 화면

`발행 현황` 맨 위 카드가 "지금 괜찮은가"에 바로 답한다.
초록이면 볼 것 없고, 빨강/노랑이면 **해야 할 일이 그 자리에 적혀 있다.**
뭘 뒤져볼 필요가 없다.

## 스스로 복구되는 것들 (손댈 필요 없음)

- **자격증명** — 대시보드를 열면 브라우저에 보관된 사본으로 서버를 자동 복구한다.
  3분마다, 그리고 앱을 다시 열 때마다 확인한다.
- **구글 토큰** — 30분마다 실제로 구글과 통신해 살아있는지 확인하고,
  끊겼으면 화면 상단에 재연결 배너를 띄운다. 발행이 실패한 뒤에야 아는 일이 없다.
- **서버 자체** — 처리되지 않은 오류가 나도 프로세스를 죽이지 않는다.
  그래도 죽으면 Railway가 무제한으로 다시 띄운다(`restartPolicyType = "always"`).
- **발행 실패** — 한 블로그가 실패해도 나머지는 그대로 올라간다.
  인증 문제로 실패한 예약 글은 버리지 않고 1시간 동안 다시 시도한다.

## 정기적으로 확인하면 좋은 것

거의 없지만, 굳이 꼽자면 두 가지다.

- 구글 OAuth 앱이 **프로덕션**인지 (테스트로 되돌아가면 7일마다 끊긴다)
- `ANTHROPIC_API_KEY` 잔액

## 개발

```bash
npm install
npm start          # http://localhost:3000
npm test           # 전체 검증 (300여 개)
node scripts/make-icons.js   # 홈화면 아이콘 다시 만들기
```
